localizable: 1.0

affordances:
    navigate-to:
        position_constraint:
            constraint: "x^2 + y^2 < 1.2^2"
#            frame: $id
#        orientation_constraint:
#            frame: $id
    place:
        height: 0.76
