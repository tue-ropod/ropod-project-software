localizable: 1.0

affordances:
    navigate_to:
        position_constraint:
            constraint: "x^2 + y^2 < 2^2"
#            frame: $id
#        orientation_constraint:
#            frame: $id
